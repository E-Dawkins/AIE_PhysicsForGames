using UnityEngine;
using UnityEngine.Events;

// Activator base class that holds a UnityEvent delegate for activation overrides
public class Activator : MonoBehaviour
{
	public UnityEvent onActivation;
}